/**
 * Global mock switch for user-details demo data.
 * Flip to false to go back to real API data everywhere mocks are wired in.
 */
export const ENABLE_USER_DETAIL_MOCKS = import.meta.env.VITE_ENABLE_MOCKS !== 'false'

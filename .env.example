# Application Environment
VITE_NODE_ENV=development

# API Configuration
# Demo runs fully on the bundled mock layer; the API prefix below is only a
# placeholder for wiring a real backend later.
VITE_SERVER=/api

# Application Version
VITE_APP_VERSION=2.8.0
